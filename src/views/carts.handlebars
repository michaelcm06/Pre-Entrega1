<!DOCTYPE html>
<html lang="es">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title> Carrito </title>

</head>

<body>
    {{{body}}}
    <h1> Carrito</h1>

    {{#each cartInfo}}

    <li>
        <h1>{{product}} </h1>
        <p>Cantidad: {{quantity}}</p>
        <p>Precio: ${{price}}</p>
        <button class="remove-button">Eliminar del Carrito</button>
    </li>

    {{/each}}

    <h2>Precio Total: ${{totalPrice}}</h2>

    <button id={{id}} class="sendTicket" style="width : 60% ; font-size : 18px">Finalizar compra</button>

    </div>

    <script src="/socket.io/socket.io.js"></script>
    <script src="/addTicket.js"></script>
      <script src="/removeFromCart.js"></script>



</body>

</html>